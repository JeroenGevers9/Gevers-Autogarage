<Window x:Class="GeversView.OrderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeversView"
        mc:Ignorable="d"
        Title="OrderView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="stackOrders" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,30,10,0" Grid.Row="0" Grid.Column="1"/>
        <DataGrid x:Name="dgOrders" AutoGenerateColumns="True"           ItemsSource="{Binding Path=DataTable, Mode=TwoWay}" Grid.Column="1"/>
    </Grid>
</Window>
